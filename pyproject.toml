[tool.poetry]
name = "python-django-diploma"
version = "0.1.0"
description = ""
authors = ["dabil <feofeonf@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^6.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
